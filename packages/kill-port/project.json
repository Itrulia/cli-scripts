{
  "root": "packages/kill-port",
  "sourceRoot": "packages/kill-port/src",
  "projectType": "library",
  "targets": {
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["packages/kill-port/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/packages/kill-port"],
      "options": {
        "jestConfig": "packages/kill-port/jest.config.ts",
        "passWithNoTests": true
      }
    }
  },
  "tags": []
}
